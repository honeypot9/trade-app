spring:
  application:
    name: datacollector
  cloud:
    config:
      uri: http://localhost:8890
      fail-fast: true
    refresh:
      enabled: false
    profiles:
        active: profile1,profile2,profile3